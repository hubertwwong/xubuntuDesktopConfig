---
# Call all other playboosk to setup my setup.
# Mostly just some ansible pratice.
- name: Run the other roles
  hosts: localhost
  roles:
    - initial_packages
    - timezone
    - dot
    - chrome
    - vscode_apt
    - python
    - git
    - docker_ce
    - mise
