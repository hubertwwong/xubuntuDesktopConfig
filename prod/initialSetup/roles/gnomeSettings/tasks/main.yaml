- name: Disable gnome lock on suspend password prompt
  shell: gsettings set org.gnome.desktop.screensaver lock-enabled false

- name: Set 12 hour format.
  shell: gsettings set org.gnome.desktop.interface clock-format '12h'